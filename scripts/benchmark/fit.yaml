defaults:
  - model: nesymres

test_path: data/datasets/20M/hdf_val_subset
name: results
benchmark_path: data/benchmark/test.csv
equation_idx: 2

hydra:
  sweep:
      dir: runs/${model.model_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  overrides:
      task:
        - equation_idx=range(0,100)  