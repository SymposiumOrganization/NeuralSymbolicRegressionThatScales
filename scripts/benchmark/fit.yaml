defaults:
  - model: nesymres

test_path: data/datasets/20M/hdf_val_subset
name: results
benchmark_name: data/benchmark/test.csv
eq: 2

hydra:
  sweep:
      dir: runs/${model.model_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  overrides:
      task:
        - eq=10,2