defaults:
  - model: nesymres

test_path: data/validation
name: results
benchmark_path: data/benchmark/nc_old.csv
equation_idx: 2

hydra:
  sweep:
      dir: runs/${model.model_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  overrides:
      task:
        - equation_idx=range(0,200)  